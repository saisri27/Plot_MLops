# Plot Decision Engine — dependencies
# install: pip install -r requirements.txt

# API
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0

# BigQuery (venues + events)
google-cloud-bigquery>=3.25.0

# Supabase / Postgres (user data + feedback) — free at supabase.com
psycopg2-binary>=2.9.9

# ML model serving
mlflow>=2.13.0
scikit-learn>=1.4.0
pandas>=2.2.0
numpy>=1.26.0

# LLM reranking (Plot Decision Engine)
openai>=1.40.0

# Utilities
python-dotenv>=1.0.0

# CI / local tests
pytest>=8.0.0

# Dev tooling (lint/format/pre-commit)
ruff>=0.15.11
pre-commit>=3.7.0
